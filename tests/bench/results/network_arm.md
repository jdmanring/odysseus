# Network arm results (generated)

Generated by `tests/bench/network_arm_bench.py` (do not hand-edit). Medians over 3 kept runs (1 warm-up discarded), `median ±(max-min)`.

Real `app.py` under uvicorn; real `/api/history`; the fork's real MessageWindow + olderLoader wiring driven by scroll. RTT emulated via CDP after app load. `detach` (#4998) is structurally 0 pages / 0 bytes on scroll-back and has no cell here — read this next to the main bench's USS tables (memory is what detach pays instead).

## Server handler latency (no browser): one cold 100-message page

- n=500: 2.15 ms ±6.69
- n=2000: 2.56 ms ±5.54

## Full walk to the oldest message — wall ms (real paging, RTT-emulated)

| n | RTT 0ms | RTT 40ms | RTT 150ms |
|---|---|---|---|
| 500 | 3602 ±1 | 3651 ±50 | 4201 ±0 |
| 2000 | 15601 ±18 | 15701 ±216 | 18451 ±0 |

## Pages fetched / bytes on the wire during the walk

| n | pages | wire kB |
|---|---|---|
| 500 | 4 | 11 |
| 2000 | 19 | 50 |

Pages fetch one at a time (the client pages on demand), so added wall time ≈ pages × RTT; the RTT columns above test that slope empirically.

