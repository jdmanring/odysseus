# Gitleaks configuration for jdmanring/odysseus fork.
# See: https://github.com/gitleaks/gitleaks#configuration

[[allowlists]]
description = "Intentional fake API key in test_log_redaction.py — tests that _redact_dict() masks sk-* values"
paths = ['''tests/test_log_redaction\.py''']
regexes = ['''sk-abc123def456''']
