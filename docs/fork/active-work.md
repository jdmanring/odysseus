# Active Work

**2026-07-07 — Upstream ingest (320 commits, `#4602`→`c67deaa6`/`#5283`) + rebase sweep.** Ran the pipeline (README protected-file conflict resolved manually; LKG-20260707-0817-MANUAL). Merged integration→develop (743 files; 18 conflicts resolved, preserving fork patches through upstream's module-extraction refactor — `tool_implementations.py`→facade, route subpackage shims). Full suite green (5381→**5392** with the fix below). Supersessions found: **#54/#57** (lazy context probe) landed upstream as **#4886/#4909**; the `?limit=400` history pagination superseded by the maintainer's history pager (direct commit **`45ee5a71`**, NOT #5090 — #5090 is only a route-subpackage refactor; corrected 2026-07-07). **That upstream pager is INERT (legacy route shadows the paginated endpoint — see recon §8); `fix/chat-history-server-paging` (merged develop `6fac912d`) is CONFIRMED built on the same broken endpoint — develop has the identical collision, so its "pagination" fetches the full history in one shot. Fix staged on `fix/chat-history-dom-eviction` commit 1; cherry-pick to develop.** **History-100 regression fixed** (`fix/chat-history-server-paging`, merged to develop `6fac912d`, folds into #2): upstream's endpoint caps a page at 100, and the fork's virtualization only walked the in-memory buffer, so long chats showed only the last 100 messages — now `chatHistory.js._fetchOlderFromServer` pages the backend (limit/offset) on scroll-up, prepends into `_all`, keeps DOM bounded (also closes the prior 400-msg ceiling); verified in-process against a real 150-msg DB (150/150 reachable), 11 new tests. **Staging branches rebased onto current `upstream-mirror`: 54 clean, 19 conflict (backed up at `backup/prerebase__*`, need the same resolutions as the develop merge — recipes in the sync report), 0 superseded-to-empty.** Nothing pushed. **Follow-ups done since:** (1) **history-100 regression fixed** (above) with real-browser Playwright verification (3 tests drive scroll→`_fetchOlderFromServer` in Chromium). (2) **cookbookRunning.js Launch button reconciled** — the merge dropped upstream's one-click Serve button from the fork's aria2c card while leaving its handler wired; re-rendered it gated by `_canLaunchDownloadedTask` + `_taskDisplayName` + `_shouldAutoExpandTaskOutput` (6 static tests incl. a render↔handler class drift guard; module is bootstrap-coupled so static-only, per fork norm; **manual QA: click Launch on a finished download**). (3) **Rebase spot-check** — 7 filing-candidate branches verified green after rebase onto current `upstream-mirror` (nvidia-nim 54, provider-logo 17, smooth-typing 5, skill-extraction 10, skill-quality 7, stream-429 15, qt-native-linux/#14 104 = 212 tests); one brittle static test fails on the **superseded** `perf/renderer-memory-reclaim` (600-char-window assertion, not a logic bug; #14 supersedes it). Full suite on develop: **5401**. (4) **#48 re-derived** — upstream did NOT rewrite the injection header (its base already had the enumerated "Do not call tools…" prohibitions #48 softens; upstream only added the orthogonal "Do not mention this wrapper" anti-leak line). Kept #48's header, appended upstream's anti-leak line verbatim; 7th test assertion; `fix/untrusted-tool-result-header` rebuilt as one clean commit on current `upstream-mirror`, **byte-identical to develop**; PR draft corrected (its old "After" contradicted the shipped header). Textual contract only — no refusal-rate eval. **Still open:** broader in-app Qt smoke-run; `pr-status.md`/submission-strategy memory rebase-state update; push. See scratch `SYNC_REPORT.md`.

Last updated: 2026-06-25. Lighter-rectangle raster tint fixed: `--enable-low-end-device-mode` removed from all three wrappers + cherry-picked to develop (#96). Controlled on/off screenshot sampling proved the flag's low-fidelity raster path (not eviction-fill / SafeOpaqueBackgroundColor — that whole theory disproved) caused a tile-aligned ~+4/+4/+5 lighter band on dark themes, and that the flag did NOT bound the real OOM. Hover memory climb fixed: idle-triggered async GC (`_scheduleIdleGc`) on perf/agent-gc-catchup + cherry-picked to develop (#97). Diagnosis: hovering interactive UI churns transient CSS `:hover` pseudo-element DOM nodes (Oilpan-managed, a separate pool from raster tiles) that embedded Chromium never GCs on idle; proven by region isolation, gc()-full-reclaim, empty V8 allocation profiler, and author-CSS-disable (+1676→+182). Validated live: hovering grew nodes +2536, idle-GC reclaimed to baseline at ~10s with no chat activity. The low-end-device toggle issue (#94) was closed as falsified (the flag it would expose helps neither the tint nor the OOM). Remote-access track filed (#95, not started). CSS animation raster-tile OOM fixed: 4 patterns in Brain and Notes panels replaced with compositor-promoted equivalents (fix/brain-panel-oom, 13 tests). Scroll-hover raster-tile accumulation fixed: transition: all override in #memory-list context (fix/memory-list-scroll-oom, 4 tests). will-change: transform removed from memory-list ::after (self-promoting animation; update to fix/brain-panel-oom). Memory list listener leak fixed: AbortController per render pass releases all item closures before innerHTML clear; document click listener moved into menuBtn handler with once:true+signal (50-listener-per-render accumulation eliminated); CSS animation-play-state:paused when panel hidden; gc() hint on close (fix/memory-panel-listener-leak, 14 tests). Abort-on-close correctness fix: modal close no longer aborts controller (no modal-opened listener → dead handlers until next memory-refresh). Skill lifecycle correctness fixed: extraction always draft, audit defaults to promote, source-aware injection pre-filter preserves teacher-escalation fast path. Skill over-extraction fixed: threshold raised (rounds >= 2 AND tools >= 3), confidence floor aligned (0.85). Agent skill prompt language fixed: mandatory pre-task consultation loop removed, unconditional authority language removed. Skill quality signals added: BM25 hybrid retrieval (0.5 Jaccard + 0.5 BM25_norm, IDF cached), composite health score (0-100) from four existing sidecar fields, color-coded health badge on skill cards. All known garbage sources addressed: DOM virtualization, streaming throttle, in-place tool bubble, deferred hljs, IntersectionObserver leak (forgetNode in all 4 pruning paths), Oilpan cooperative GC hint, OS pressure monitors, timer leak, renderTail holder-div allocation (text-only path), agent-path double-allocation (finalize in-place), rewrite O(n^2) per-token rebuild, multi-round agent double rebuild (tool_start + finalize), continue-button WeakRef closure retention. CDP post-evict listener audit in qt_wrapper. renderProcessPid, ThreadPoolExecutor, startup log rotation added. NameError in threshold-gc path fixed. Log rotation constants aligned with app (10 MB / 5 backups). GC scheduling improved: _gcMissed catch-up flag eliminates agent-session blind spot. Typing performance: synchronous CDP purge replaced with async JS gc() (eliminates 1-second input freezes); autoResize replaced with rAF-coalesced height:auto+scrollHeight (eliminates layout thrashing). GC micro-improvements (session 5): squashOutsideCode fast path, deferred hljs on all 8 sites, rIC after _evictLive/_pruneBottom, full teardown in _pruneTop/_pruneBottom, purge-stale-streams on session switch, V8+Chromium memory flags. 4527 tests pass. Remaining: in-app verification with a long agent session; macOS/Windows hardware tests. Research doc: docs/fork/memory-explosion-research.md.

---

**2026-07-08 — Chat-history architecture: decided + audited.** The fork's `MessageWindow`
(`chatHistory.js`, DOM eviction + bidirectional server paging) is kept on `develop`: it bounds RSS
(true node removal), which upstream does not — the merged pager (`45ee5a71`) is prepend-only, and
**both open upstream PRs miss the memory bound** (#4661 paging-only; #4998 `chatVirtualizer.js` detaches
children but keeps them in heap = lag fix, not RSS). Open issue **#4644** asks for DOM removal —
attach our eviction PR there (issue-first). If #4998 merges it becomes a substrate → contribute
eviction on top, retire most of `MessageWindow`. Upstream research: ROADMAP/issues/PRs/discussions
surveyed; CONTRIBUTING constraints captured (agent PRs issue-first + human-filed, screenshots, no emoji).
**Gold-standard audit done:** mechanical core matches best practice; fixed the a11y weakness (aria-busy
bracket composing with streaming, focus-preserve on eviction, sentinel keyboard access, aria-hidden
decoratives) each guard-tested; deleted the dead upstream pager cluster from `sessions.js`. **New tests
(161 green together):** `test_history_pagination_contract.py` (9: pagination math, hidden/base64
stripping, route-shadowing guard), `test_chat_history_a11y_js.py` (7), extended render-path Playwright
with markdown/code/image. Full story + comparison table + when-to-revisit:
`docs/fork/chat-history-architecture-decision.md`.

## In Progress

| Issue | Branch | Status |
|-------|--------|--------|
| [#64 streaming throttle OOM](https://github.com/jdmanring/odysseus/issues/64) | `fix/dom-oom-streaming-throttle` | **Built and pushed.** From upstream-mirror. Fixes: thinking-block textContent during streaming (kills O(n²) mdToHtml), rAF throttle for normal streaming, StreamRenderer closure teardown after final render, background stream accumulated text cleared on completion, idle GC yield in finally block. |
| [#54 agent context budget locked at 6K](https://github.com/jdmanring/odysseus/issues/54) | `fix/agent-context-budget-discovery` | **Practical impact addressed by #56.** The `api`/`proxy` early-return is upstream-authored (commit `a2e691da`) — deliberately kept. Branch adds one test. See [#57](https://github.com/jdmanring/odysseus/issues/57) for lazy-probe research. |
| [#57 Research: lazy context-length probe for api/proxy endpoints](https://github.com/jdmanring/odysseus/issues/57) | no branch | **Research required before building.** Survey which commercial providers return context fields; verify manual refresh button / `_context_cache` interaction; audit probe field coverage. Draft: `docs/fork/upstream/issue-drafts/feat-lazy-context-probe.md`. |
| [#63 stream_options compatibility unknown for 8+ providers](https://github.com/jdmanring/odysseus/issues/63) | no branch | **Empirical testing required.** NVIDIA NIM, Venice, Perplexity, MiniMax, Zhipu, Kimi, Pollinations, Cohere — all have unknown stream_options support. Current exclusion list: `{"openrouter", "groq", "longcat"}`. Send `stream_options: {include_usage: true}` to each and record whether they accept or 400/422. |
| [#55 429 treated as hard failure, no backoff](https://github.com/jdmanring/odysseus/issues/55) | `fix/stream-429-backoff` | **Complete.** Branch committed (75f1f01e) and pushed. 15 tests pass. PR draft: `docs/fork/upstream/pr-drafts/fix-stream-429-backoff.md`. |
| [#56 NVIDIA NIM gaps (no curated list, wrong/missing context windows)](https://github.com/jdmanring/odysseus/issues/56) | `feat/nvidia-nim-support` | **Complete.** Branch committed (d620c5b3) and pushed. 87 tests pass. PR draft: `docs/fork/upstream/pr-drafts/feat-nvidia-nim-support.md`. |
| [#36 hf_transfer removal](https://github.com/jdmanring/odysseus/issues/36) | not started | Strip all hf_transfer install/enable code. Blocked pending upstream acceptance of feat/aria2c-downloader. |
| [#40 Google compat layer hardening](https://github.com/jdmanring/odysseus/issues/40) | not started | `_is_google_compat()`, `_build_google_compat_payload()`, extract streaming quirk helpers, add tests for all four known quirks. Native API path explicitly out of scope. |
| [#41 Gemma 4 local serving format](https://github.com/jdmanring/odysseus/issues/41) | not started | Low priority. Raw `<\|tool_call>` tokens leak only through self-hosted Ollama/llama-cpp-python/mlx-lm when chat template is incomplete. Google's hosted API is unaffected (already translates to `<tool_code>` format). |
| [#42 Expanded quality-scored resolver — all backends + imatrix tiers](https://github.com/jdmanring/odysseus/issues/42) | not started | Depends on `feat/aria2c-downloader` and `fix/gguf-quality-scored` landing upstream. Scope: add imatrix variants to QUANT_HIERARCHY, extend HfUrlResolver with find_vllm_sources(), wire resolver into all download paths (not just missing-config fallback). |
| [#123 persist catalog↔download association (retire base-name heuristic)](https://github.com/jdmanring/odysseus/issues/123) | not started (from `upstream-mirror`) | **Designed, not started.** Root cause behind [#121](https://github.com/jdmanring/odysseus/issues/121)'s client base-name heuristic (the documented stopgap). Senior design lives in the issue: capture download **provenance** at download-complete time, store it as update-surviving **user state** (NOT in `hf_models.json`, which is shippable reference data wiped on update, see upstream #4049), match by **exact join**, give discovered entries stable ids; keep the base-name heuristic only as a one-shot backfill that then retires. Relates to upstream #4049 / #2342. Plan: the issue body. |
| [#43 macOS native wrapper](https://github.com/jdmanring/odysseus/issues/43) | `feat/qt-native-macos-app` | build-mac-app.sh done (.app + .dmg). `mac_wrapper.py` built (`6b1ee5e8`): vm_stat compression-delta pressure monitor, QColorDialog (NSColorPanel), ps-based RSS, CDP+focus-loss purge. P1-P5 perf commits: `--enable-low-end-device-mode`, HTTP cache cap, lifecycle freeze on minimize, timer 30s, RSS before/after telemetry (`fbb466c5`). Cherry-picked to develop. **`--enable-low-end-device-mode` later removed — see #96.** Pending macOS hardware test. |
| [#44 Windows native wrapper](https://github.com/jdmanring/odysseus/issues/44) | `feat/qt-native-windows-app` | build-windows-app.ps1 + install.bat done. `windows_wrapper.py` built (`bcc05cc4`): `CreateMemoryResourceNotification` kernel pressure monitor, QColorDialog, tasklist RSS, CDP+focus-loss purge. P1-P5 perf commits: `--enable-low-end-device-mode`, HTTP cache cap, lifecycle freeze on minimize, timer 30s, RSS telemetry via tasklist (`88601fca`). Cherry-picked to develop. **`--enable-low-end-device-mode` later removed — see #96.** Pending Windows hardware test. |
| [#90 js-flags space-splitting fix](https://github.com/jdmanring/odysseus/issues/90) | `feat/qt-native-linux-app` + mac/windows | **Done.** Commit `14928a5d` on `feat/qt-native-linux-app`, `0570769d` on mac, `a856ba28` on windows. Cherry-picked to develop. All 3 wrappers now use comma-separated `--js-flags=--expose-gc,--initial-old-space-size=128,...` so all V8 flags reach the renderer. 2 new tests (`test_js_flags_comma_separated`, `test_js_flags_no_space_after_expose_gc`). Pending: `HeapProfiler.collectGarbage` on idle eviction path (tracked in #91). |
| [#106 renderer memory climbs unbounded — reclaim is a no-op](https://github.com/jdmanring/odysseus/issues/106) | `perf/renderer-memory-reclaim` (stacks on `feat/qt-native-linux-app`) | **Implemented; in-app verification pending.** Live CDP measured: `simulatePressureNotification('critical')` is a no-op on QtWebEngine (RSS unchanged), while `forciblyPurgeJavaScriptMemory` reclaimed 5.2 GB / 3.7 GB. The wrapper's idle/periodic/focus-loss triggers were calling the no-op, so the renderer climbed to 5+ GB with panels open while `gc()` only touched the 43 MB JS pool. Fix: gated `_purge_renderer` (RSS ceiling 1.8 GB, 15 s rate limit, mouse-idle + focus-loss only, in the CDP executor); periodic timer is telemetry only. Commit `da9a3dfa`, cherry-picked to develop (`ea119933`, `-x`). 68 wrapper audit tests pass. **Verify:** restart app, watch `logs/wrapper_system.log` for `[MEM] forcible purge (...): ok RSS X -> Y (delta=-N)`. Layer 3 of `docs/fork/memory-management-architecture.md`; plan at `docs/fork/plans/memory-management-plan.md`. Follow-ups: mac/windows parity; layer 2 (panel teardown/residency); 1s purge stutter is intrinsic, hence the off-path gating. **Update (`3151ed14`/develop `ab045689`): reclaim was single-shot (re-armed only on mouse move) so a walk-away filled all RAM; added a repeating sustained-idle timer (4 s, purge if no input ≥3 s, keyboard-aware via `_InputIdleFilter`) so memory is bounded regardless. Measured two idle producers: ~1.7 MB/s perpetual CSS animations (dominant `memory-synapse-sweep`, 21 memory rows) + ~3.1 MB/s non-animation repaint (unidentified, needs live re-measure). 70 wrapper tests pass. See architecture doc 2026-06-25 follow-up.** |
| [#107 whirlpool spinner rAF loop never terminates (orphaned/hidden)](https://github.com/jdmanring/odysseus/issues/107) | `fix/spinner-orphan-leak` | **Implemented; in-app re-measure pending.** Read-only rAF caller-stack capture named it: `Spinner._drawWhirlpool` (spinner.js:256) scheduling ~43 rAF/s with 0 canvas elements in the DOM — an orphaned spinner looping forever (the unbounded `!_wpWasConnected` grace). This is the ~3.1 MB/s non-animation producer + main-thread saturation. Fix: gate on visibility (`isConnected && offsetParent`) and bound the never-visible grace to `_WP_ORPHAN_GRACE_FRAMES` (~2s); also stops display:none-panel spinners. Commit `307028e9`, cherry-picked to develop (`-x`). 5 tests in `tests/test_spinner_orphan_leak_js.py`. Evidence: `docs/fork/memory-investigation-evidence.md` §5. |
| [#109 UI unresponsive after minimize/restore — page freeze loses input](https://github.com/jdmanring/odysseus/issues/109) | `perf/renderer-memory-reclaim` | **Implemented; verify in-app.** Minimize froze the page (`setLifecycleState(Frozen)`); the Frozen→Active thaw left the web UI unresponsive (tabs unmanipulable until close/reopen). Confirmed by Qt docs ("a visible page must remain Active"; a non-Active page can lose HTML input) + PyQt's unreliable lifecycle transitions. Fix: remove the freeze entirely; keep the page Active and reclaim on minimize via the gated `_purge_renderer('minimized')`. Commit `e0169457`, cherry-picked to develop (`a65686f5`, `-x`). Audit test updated. |
| [#110 Tasks clock re-rasters the whole modal layer every second (~1.7 MB/s)](https://github.com/jdmanring/odysseus/issues/110) | `fix/tasks-clock-repaint` | **Implemented + verified flat-when-idle.** The draggable Tasks modal is its own compositor layer; the clock's 1/sec `textContent` repaint re-rastered the whole ~600×848 modal texture (reclaimable cache, not a leak — a purge-recheck reclaimed it). **Two-part fix:** (1) CSS isolation of `.tasks-clock` to its own layer (`transform: translateZ(0)` + `contain: layout paint`, `ece4a44a`) — but this cut the per-repaint *area*, not the per-second *frequency*, so the climb persisted; (2) **minute-resolution clock** (`436ee61a`): `_tickClock` drops seconds and writes `textContent` only when the string changes, so all but ~1 tick/min is a no-op — this is what actually eliminates the producer. Live-verified: clock renders `'… · 06:09 PM'`, nothing in the codebase reads the clock content/seconds, panel functional. Cherry-picked to develop (`-x`). Tests: `test_tasks_clock_repaint_css.py` (CSS) + `test_tasks_clock_js.py` (minute-res + write-on-change). **Verified result:** idle slope 0.00 MB/s, active use climbs transiently then the idle reclaim drops it to a ~430 MB baseline (sawtooth, leak-free, stall-free). Reclaim ceiling also lowered 1.8→1.2 GB (`fbd243c1` on `perf/renderer-memory-reclaim`). |
| [#108 Brain/Notes CSS animations generate continuous tiles](https://github.com/jdmanring/odysseus/issues/108) | `fix/brain-panel-oom` | **Implemented; in-app re-measure pending.** Extends existing brain-panel-oom branch. `memory-synapse-sweep` ran `infinite` on ~20 memory rows (the ~1.7 MB/s animation producer; cancelling all CSS animations live dropped the slope by that much). Made it **hover-triggered** (single iteration on hover; nothing at rest, strip parked off-screen), respecting `prefers-reduced-motion` — keeps the visual, kills ~20 perpetual animations. 4th commit `1fa3f23d`, cherry-picked to develop (`-x`, resolved conflict with the memory-list-scroll-oom hidden-pause rule). 13 tests in `tests/test_brain_panel_oom_css.py`. Evidence: `docs/fork/memory-investigation-evidence.md` §6. |
| [#91 V8 heap telemetry + host RSS](https://github.com/jdmanring/odysseus/issues/91) | `feat/qt-native-linux-app` | **Not started.** Add `Runtime.getHeapUsage` CDP call to `_log_renderer_memory` (`[V8] heap: used=N MB total=N MB`). Add Python/Qt host process RSS to periodic log. Extends issue #14 branch. See Session 9 Phase 1 in research doc. |
| [#120 qt_wrapper graduated PSI monitor (some/full, tunable thresholds)](https://github.com/jdmanring/odysseus/issues/120) | `perf/qt-psi-graduated-reclaim` → **folded into `feat/qt-native-linux-app` (#14)** | **Implemented; verified against live kernel PSI.** Graduated some/full level map (MODERATE → async GC; CRITICAL → gated #106 `_purge_renderer`), env-tunable thresholds (`ODYSSEUS_PSI_*`, default 10/40/5), one `[PSI]` telemetry line per transition (level, some, full, host `mem_avail_mb`, `rss_mb`, `swap_mb`, action) to field-validate the table. **Detection logic extracted into Qt-free `qt_psi.py`** (detection-core / output-adapter split) — this fixed a real defect: `test_psi_monitor.py` had been *silently skipped* because the server venv has only a PyQt stub, so the logic had zero executing coverage; now it runs (no skip, 11 unit tests). Smoke-tested with `stress-ng`: NONE→MODERATE→NONE with correlated MemAvailable/swap. Commits `6ccc0f9d`/`803b4361`, cherry-picked to develop (`9e6ca024`/`1f85aaa6`); **folded into #14 (2026-06-26)** for upstream since `qt_wrapper.py` is a new file (ships memory-managed, not patched after). Same validated PSI logic as the `chromium-linux-mempressure` evaluator. Plan: `docs/fork/plans/psi-graduated-reclaim-plan.md`. See [[project_venv_pyqt_stub]]. |
| [#92 CSS hover raster — global containment + animation fix](https://github.com/jdmanring/odysseus/issues/92) | `fix/css-hover-raster-global` | **Not started.** Three causes of global hover memory growth: `email-notif-breathe` animates box-shadow (not compositor-promoted); 77 `transition: all` occurrences trigger non-compositor transitions on all hover events. Fixes: replace box-shadow with opacity in `email-notif-breathe`; replace `transition: all` with specific properties on `.icon-rail-btn`, `.input-icon-btn`, `.section-header-btn`, `.tab-btn`. Note: `.sidebar` and `.chat-container` containment addressed by #93 (`contain: layout style` on sidebar, chat-history). Branch from `upstream-mirror`. See Session 9 in research doc. |
| [#105 chat image generation undiscoverable (inpaint-scoped settings)](https://github.com/jdmanring/odysseus/issues/105) | no branch yet | **Not started.** `generate_image` works (cloud `gpt-image`/`dall-e` auto-detect, or local `model_type=image` endpoint), but Settings → Image Generation is inpaint-scoped: hardcoded SD list shown "(not detected)", no real provider/model option, and it writes the same `image_model` key chat generation reads (`settings.js:794` ↔ `ai_interaction.py:910`). Enabled-but-unconfigured fails silently. Gate at `agent_loop.py:1402`. Image endpoints are registerable via Admin (`index.html:2094`) but never surfaced. Direction: separate inpaint vs generation model, list real options, surface a reason when unconfigured. Upstream-candidate; branch from `upstream-mirror`. |
| [#93 CSS contain:paint breaks backdrop-filter + transparent compositing](https://github.com/jdmanring/odysseus/issues/93) | `fix/css-contain-paint-transparent-rendering` | **Done.** Commit `dac00cf0` on upstream-candidate branch. Applied to develop as `b1b2e37a` (changes `contain: content` → `contain: layout style` on `.sidebar` and `.chat-history`). Fixes: (1) `contain: paint` broke `backdrop-filter` on the frosted-theme sidebar; (2) transparent `.chat-history` with paint containment rendered opaque in Qt compositor with `--enable-low-end-device-mode` tile budget. Updated `test_css_render_perf.py` (2 assertions). 4 new tests in `tests/test_css_contain_paint_transparent.py` on upstream-candidate branch. |
| [#96 remove --enable-low-end-device-mode (raster tint, not an OOM fix)](https://github.com/jdmanring/odysseus/issues/96) | `feat/qt-native-linux-app` + mac/windows | **Done.** Commits `4b3b175b` (linux, inverts the audit test to assert flag absence), `2a8810f8` (mac), `5f205919` (windows); cherry-picked to develop (`657d0015`/`b652256e`/`d9f9dc15`, `-x`). Develop also carries `a8a0e3ec` keeping the harmless explicit-`--bg`/setBackgroundColor code while correcting the disproven eviction-fill rationale in `test_css_render_perf.py`, `test_memory_list_scroll_oom_css.py`, `test_qt_cdp_listener_audit.py`. Controlled on/off screenshot sampling proved the flag's low-fidelity raster path caused the tile-aligned lighter band; `page.setBackgroundColor()` (already `#1e1e2e`) had no effect, and `--disable-low-res-tiling` did not fix it. The earlier #93/contain + html-bg eviction-fill theory is disproven (kept only as harmless theming hygiene). File upstream issue first. |
| [#97 idle-triggered GC for transient hover DOM churn](https://github.com/jdmanring/odysseus/issues/97) | `perf/agent-gc-catchup` | **Done.** Commit `5f4c3347` on `perf/agent-gc-catchup`; cherry-picked to develop (`a6b3fad2`, `-x`). `_scheduleIdleGc()` fires one async-major `gc()` ~8s after the last pointer/keyboard input — shares `_gcPending` (never stacks with the post-response cycle), visibility-gated, feature-detected (no-op without `--expose-gc`), passive+capture listeners. Root cause: hovering interactive UI churns transient CSS `:hover` pseudo-element DOM nodes (Oilpan, a separate pool from raster tiles) that embedded Chromium never GCs on idle; proven by region isolation, gc()-full-reclaim, empty V8 allocation profiler, author-CSS-disable (+1676→+182). Validated live: hover grew nodes +2536, idle-GC reclaimed to baseline at ~10s with no chat activity. 6 new guards in `test_chat_gc_hint_js.py`. Builds on #80's GC machinery. File upstream issue first. |
| [#98 lazy-decode off-screen stacked/grid images](https://github.com/jdmanring/odysseus/issues/98) | `perf/image-lazy-decode` | **Done.** Commit `686ba958` from `upstream-mirror`; cherry-picked to develop (`-x`). Adds `loading="lazy"` + `decoding="async"` to the document multi-page PNG stack (`document.js`) and the gallery draft-thumb grid (`gallery.js`) — the two renderers that emit many off-screen images at once. Deliberately narrow: gallery main grids were already lazy, chat images are DOM-virtualized, focus/detail images stay eager (guarded by a test). First item from `docs/fork/perf-audit-2026-06.md` (finding A1). 4 guards in `tests/test_image_lazy_decode.py`. File upstream issue first. |
| [#99 gzip-compress aged-out editor undo snapshots](https://github.com/jdmanring/odysseus/issues/99) | `perf/editor-undo-compress` | **Done + smoke-tested.** Commits `3a310c9b` (code + 10 source-text guards), `62aff4b7` (live codec test) from `upstream-mirror`; cherry-picked to develop (`-x`). galleryEditor undo snapshots store every layer's/mask's/wand's full raw ImageData per op (kept ≤ MAX_HISTORY=20) — a large transient edit-session peak. Compress snapshots outside a 3-deep raw window in `requestIdleCallback`; decode on demand for deep undo. **Codec: gzip (`CompressionStream`) over raw bytes, NOT PNG** — PNG-via-canvas premultiplies alpha and drifts partial-alpha pixels; gzip is byte-exact (verified live). Recent snapshots raw → sync/instant undo; deep undo async, `_restoreGen`-guarded; atomic raw→gz commit; no-op if `CompressionStream` absent. Smoke-test (4 edits + crop undo/redo) clean, no async errors in session log. Audit finding A2. 10 guards in `tests/test_editor_undo_compression.py` + 1 live test in `tests/test_editor_undo_compression_integration.py`. File upstream issue first. |
| [#100 Ctrl+Shift+Z redo shortcut dead](https://github.com/jdmanring/odysseus/issues/100) | `fix/editor-redo-shortcut` | **Done.** Commit from `upstream-mirror`; cherry-picked to develop (`50ed792d`). `keyboard-shortcuts.js` checked `e.key === 'z'`, but with Shift held `e.key` is uppercase `'Z'`, so the redo branch never fired (plain Ctrl+Z worked). Now `e.key === 'z' \|\| e.key === 'Z'`, matching the file's other Shift chords. 2 guards in `tests/test_editor_redo_shortcut.py`. File upstream issue first. |
| [#101 editor save can create 0-byte broken gallery image](https://github.com/jdmanring/odysseus/issues/101) | `fix/editor-empty-save-guard` | **Done.** Commit from `upstream-mirror`; cherry-picked to develop (`5f0cb3b9`). Both save paths (`ge-save` replace + `exportToGallery` save-copy) flattened+uploaded with no empty-output guard; a save firing before the image loaded / after a state reset wrote a broken 0-byte gallery entry (observed live: null×null, 0 bytes — deleted). Added `_flattenForSave()` (throws on 0×0) + an empty/trivial-blob reject in `toBlob`, surfaced as an error toast. 3 guards in `tests/test_editor_empty_save_guard.py`. File upstream issue first. |
| [#102 persist editor undo history across sessions](https://github.com/jdmanring/odysseus/issues/102) | no branch | **Deferred to its own session.** The editor resets `undoStack` on open, so reopening a draft loses undo history (the current edited image is restored, just not the history — by design, not a regression; #99 is unrelated). Full-history persistence is **rejected**: flattened photo layers don't gzip (≈0.997 under #99), so persisting all ~20 snapshots ≈ 20× the image payload on every debounced save — a save-latency/DB-bloat regression. **Recommended: bounded tail** (last N, size-capped, gzip→base64 in draft payload v3, clone-not-mutate, backward-compatible). ~200 lines of correctness-critical async serialization; cross-session restore needs the live editor. Stacks on `perf/editor-undo-compress` (#99). Full design + cost analysis in the issue. |
| [#95 network-reachable mode for remote/tablet access](https://github.com/jdmanring/odysseus/issues/95) | no branch | **Not started.** Server-side track (no engine work): wrapper currently binds uvicorn to `127.0.0.1` only. Make network bind opt-in (`0.0.0.0`/configured iface, localhost-only default), require auth when exposed (audit existing api-token + `127.0.0.1`/`::1` trust checks + forwarded-header handling), document TLS, verify `ALLOWED_ORIGINS`. UI is already responsive (`@media (max-width:768px)`); a remote tablet renders in its own OS-managed browser, so none of the wrapper's memory/raster workarounds apply. |
| [#127 MessageWindow scrollbar misrepresents history (no spacer for unrendered range)](https://github.com/jdmanring/odysseus/issues/127) | `fix/chat-history-scrollbar-spacer` | **UPSTREAM-CANDIDATE. Fixed + verified on develop (merges `b28a5aca` + `2301dca5`); issue stays open until #2's upstream PR is filed.** Estimator spacers own the whole unrendered range, BOTH edges (a top-only first cut collapsed back to 4% at walk end — caught by the walk-stability assertion pre-commit). Anti-drift per #126: idempotent recompute, exact prune-time heights keyed by absolute DB index, estimator average moves only in compensated contexts. Measured honesty at load: 1.01/1.00/1.00 for n=300/2000/5000 vs walk-end ground truth (was 0.50/0.03/0.01); uniform-corpus caveat recorded. Two follow-up defects caught by verification, fixed same cycle: double compensation with native scroll anchoring (+36 px boundary jump → anchor-restore, 0 px) and catch-up chain over-triggering (proximity margin → blank-in-view gate). Regression: honesty scaling + walk stability (red-verified 0.97) + 4 anti-drift static guards; #129/#130 regressions green; both full suites exit-0. **Staged #2 branch converged to the maintained chatHistory.js + test pairing (`463526b0`)** — the file-portion of the Part 4.5 fold done early, fork refs scrubbed; server paging inert there until wired. First merge landed on a broken suite gate (piped exit code) — caught, two real defects fixed forward, gate corrected. |
| [#128 chat-history virtualization benchmark](https://github.com/jdmanring/odysseus/issues/128) | `bench/chat-history-virtualization` | Harness + vendored arms (fork MessageWindow, upstream #4998 virtualizer, hybrid) with snapshot-drift guard. Results live only in generated `tests/bench/results/bench.md`. **Network arm DONE (2026-07-17, `4ae9c699`):** `tests/bench/network_arm_bench.py` — real `app.py`/uvicorn/`/api/history`/MessageWindow wiring, RTT via CDP; deep scroll-back cost = serialized pages × RTT (confirmed at n=500 and n=2000), handler ~2 ms/page, full 2000-msg walk 19 req/~50 kB gzipped; #4998's zero-network win is real but does not dominate (numbers in generated `network_arm.md`). Building it exposed [#129](https://github.com/jdmanring/odysseus/issues/129)/[#130](https://github.com/jdmanring/odysseus/issues/130). **Still unmeasured:** `measureUAM` DOM-bytes corroboration; QtWebEngine ecological validity. **Shared infra grew (2026-07-17):** `tests/bench/io_probe.js` (`window.ioProbe`, the graduated IO-delivery diagnostic that caught #130) + executing unit tests, joining `live_app.py`/`scroll_driver.js`. |
| [#129 DOM grows unbounded during sustained scroll-up (real app)](https://github.com/jdmanring/odysseus/issues/129) | `fix/chat-history-scrollup-prune` | **UPSTREAM-CANDIDATE (misclassified fork-only at first — chatHistory.js is staged upstream via #2). Fixed + verified on develop `dd645129`; issue REOPENED, stays open until #2's upstream PR is filed with the fix folded in (plan Part 4.5).** Root cause was NOT prune starvation (that hypothesis was wrong): `_fetchOlderFromServer` shifted the `_all` index space on prepend without retagging rendered `data-ch-idx`, so tags collided across pages (walk left tags 0–99 vs `_endIdx` 280) and the Phase-3 prune broke at the first stale tag. Fix: retag all rendered nodes on prepend. Real-app n=2000 walk: exactly 80 msgs in DOM (was ~1,980), coherent tags; scroll-down reload verified clean. Regression test: constant bound (130) + tag-coherence (`maxTag == _endIdx-1`, the load-bearing assertion), red-verified. Snapshot re-vendored; evict-arm cell unchanged (720 vs ~718). Full suite 5432. Decision-record premise restored (addendum updated). **Staged upstream eviction audited: immune** — `_tagHistoryOffset` uses absolute DB offsets, which never shift on prepend (evidence on the issue). Network-arm artifact refreshed post-fix (`2d30e275`). |
| [#103 scroll-down drains to bottom instead of inverse windowing](https://github.com/jdmanring/odysseus/issues/103) | (fixed via `8fc0dcdc`, 2026-06-25) | **UPSTREAM-CANDIDATE. Verified fixed (2026-07-18); issue stays open until #2's PR is filed.** The prescribed fix had been in the code since 2026-06-25 — the issue lagged it. Real-app behavioral verification surfaced #132 on the same path; with that fixed, one trigger = one batch (80→105), no drain, no yank. Plan item 3.1 marked done. |
| [#132 scroll-down batch renders below the bottom honesty spacer](https://github.com/jdmanring/odysseus/issues/132) | `fix/chat-history-loadnewer-insert-order` | **UPSTREAM-CANDIDATE. Fixed + verified on develop `4cf6325b`; stays open until #2's PR is filed.** #127 regression: `_loadNewer` inserted before `_histSep`, which now sits below the bottom spacer — batches rendered under the blank representing them (order corrupted; 125 msgs on one trigger). Fix: insert at the content edge (bSentinel → botSpacer → histSep fallback). Regression test: DOM-order coherence + nothing below the spacer, red-verified. **The #131 convergence guard fired on its first live drift** and forced the staged-branch re-convergence in-cycle (143 branch tests green). Full suite 5,450 exit-0. |
| [#130 scroll-up paging dead-ends at pinned top (one-shot IO fire swallowed)](https://github.com/jdmanring/odysseus/issues/130) | `fix/chat-history-pinned-top-paging` | **UPSTREAM-CANDIDATE. Fixed + verified on develop; issue stays open until #2's upstream PR is filed with the fix folded in (plan Part 4.5).** Root cause was NOT the one-shot disconnect dance the issue suspected: the sentinel IO callback read `entries[0]` — the OLDEST queued entry. Under a busy main thread the sentinel leaves+re-enters before delivery, one callback carries `[leave, enter]`, the stale leave is read, the enter discarded, and no transition ever follows (captured live via instrumented observer: one delivery with `isIntersecting [false, true]`). Fix: read `entries[entries.length-1]`. Verified: 9-cell probe sweep (RTT × pin-cadence, n=2000, real paging) — 2 cells dead-ended pre-fix with the exact stall signature, all 9 complete post-fix. Regression: `test_pinned_top_walk_completes` (pinnedTopWalk @30 ms cadence, 40 ms RTT — the provoking shape; race, so red is probabilistic) + deterministic static guard `test_sentinel_observer_reads_newest_entry`. No other `entries[0]` IO consumers in `static/js/`. Staged eviction branch unaffected (no IO). Snapshot re-vendored; evict cell unchanged (720 vs 718). **Fix ALSO applied directly to `fix/dom-oom-virtualization` (`661be326`) — the defect was live in the staged snapshot (no server paging needed); its static guard is on that branch, worded without fork issue numbers.** |
| [#45 FreeBSD (KDE Plasma) wrapper](https://github.com/jdmanring/odysseus/issues/45) | `feat/qt-native-freebsd-app` | build-freebsd-app.sh and install.sh done. qt_wrapper.py platform guard pending FreeBSD hardware. Plan at `docs/fork/plans/freebsd-wrapper-plan.md`. |
| [#46 OpenBSD native wrapper](https://github.com/jdmanring/odysseus/issues/46) | `feat/qt-native-openbsd-app` | build-openbsd-app.sh done. qt_wrapper.py pkill/pgrep guards pending OpenBSD hardware. Plan at `docs/fork/plans/openbsd-wrapper-plan.md`. |

---

## Staged for Upstream — Ready to File

All branches are built from `upstream-mirror`, contain only their specific changes, and are
pushed to `origin`. See `docs/fork/upstream/pr-status.md` for
full status and `docs/fork/upstream/pr-drafts/` for draft descriptions.

| Branch | Issue(s) | Notes |
|--------|----------|-------|
| `fix/memory-panel-listener-leak` | [#89](https://github.com/jdmanring/odysseus/issues/89) | 2 commits: primary fix (`d89d93a6`), abort-on-close correctness fix (`ab8a5f21`). From upstream-mirror. Eliminates three sources of ~956 MiB permanent RSS growth in the Brain memory list: (1) document.addEventListener accumulation — moved from forEach body (N listeners per render) to menuBtn handler with { once: true, signal }; (2) item-level listener closure retention — AbortController per render pass, abort before innerHTML clear; (3) compositor tiles while panel hidden — CSS animation-play-state:paused rule. MutationObserver triggers _closeActiveDropdown() + gc() on panel close. odysseus:modal-closed event added to modalManager.js. Abort-on-close removed (no modal-opened listener → would leave dead handlers until next memory-refresh). 14 regression tests in `tests/test_memory_panel_listener_leak.py`. PR draft at `docs/fork/upstream/pr-drafts/fix-memory-panel-listener-leak.md`. File upstream issue first. |
| `fix/brain-panel-oom` | TBD (file before submitting) | 3 commits: fix (`8e3c1674`), comment+test cleanup (`02d98263`), remove will-change (`18dbbd25`). From upstream-mirror. Eliminates raster-tile accumulation from 4 CSS animation patterns in Brain and Notes panels. Pattern A: @property --sweep replaced with transform:translateX(); Pattern B: filter removed from note-ai-shine; Pattern C: animation:none replaced with animation-play-state:paused on notes-quick-add; Pattern D: background-position replaced with transform in notes-drag-shimmer. will-change: transform removed from ::after (transform animation is self-promoting; pre-promoting all list items including off-screen wastes GPU backing textures). 13 regression tests in `tests/test_brain_panel_oom_css.py`. PR draft at `docs/fork/upstream/pr-drafts/fix-brain-panel-oom.md`. File upstream issue first. |
| `fix/memory-list-scroll-oom` | [#88](https://github.com/jdmanring/odysseus/issues/88) | 6 commits: transition fix (`f43c69c2`), hover paint suppression (`90e21d62`), always-visible buttons (`ca2bcc27`); P6 content-visibility:auto on memory/email/doclib/task list items + contain-intrinsic-size (`f61c4569`); P7 revert hover/button paint suppressions — restore hover UX, keep transition:opacity override (`f61c4569`); test update (`2d0f76ba`). From upstream-mirror. Phase 1: `transition: all → opacity` eliminates ~9 tile frames per hover event. Phases 2/3 previously suppressed hover visual — reverted now that tile budget (`--enable-low-end-device-mode`) + content-visibility are in place. content-visibility:auto prevents off-screen list items from generating raster tiles. 12 tests in `tests/test_content_visibility_css.py`, 8 tests in `tests/test_memory_list_scroll_oom_css.py`. PR draft at `docs/fork/upstream/pr-drafts/fix-memory-list-scroll-oom.md`. File upstream issue first. |
| `fix/dom-oom-streaming-throttle` | [#64](https://github.com/jdmanring/odysseus/issues/64) | 3 commits: fix (`d35f3819`), tests (`6cae1aad`), PR draft (`16e8bc16`). From upstream-mirror. Thinking-block textContent throttle (O(n²) fix), rAF throttle for normal streaming, StreamRenderer teardown, background stream cleanup, idle GC yield. 14 static-analysis tests in `tests/test_chat_streaming_oom.py`. PR draft at `docs/fork/upstream/pr-drafts/fix-dom-oom-streaming-throttle.md`. File upstream issue first. |
| `fix/provider-logo-ordering` | [#59](https://github.com/jdmanring/odysseus/issues/59) | Single commit (`a35b1b1d`), ready. Adds Groq, Together (evenodd), Fireworks, Pollinations logos; moves Google Gemini before OpenAI entry (first-match ordering bug); fixes xAI regex; removes duplicate Ollama Cloud. 17 JS logo tests pass. File upstream issue first. |
| `fix/nvidia-native-tool-calling` | [#60](https://github.com/jdmanring/odysseus/issues/60) | Single commit (`116bb913`), ready. Adds `integrate.api.nvidia.com` to `_API_HOSTS` and `"nemotron"` keyword to `_model_supports_tools`. File upstream issue first. |
| `fix/api-hosts-provider-gaps` | [#62](https://github.com/jdmanring/odysseus/issues/62) | Single commit (`4047e40a`), ready. Adds `generativelanguage.googleapis.com`, `pollinations.ai`, `moonshot.cn`, `together.ai`, `bigmodel.cn` to `_API_HOSTS`. File upstream issue first. |
| `feat/longcat-provider` | [#58](https://github.com/jdmanring/odysseus/issues/58), [#61](https://github.com/jdmanring/odysseus/issues/61) | 3 commits — logo (`212b5099`), ordering fix (`1b7f04b3`), max_tokens+stream_options (`fae6ae6d`). `_PROVIDER_DEFAULT_MAX_OUTPUT["longcat"] = 131072`; excluded from stream_options. 11 tests pass. File upstream issue first. Source: https://longcat.chat/platform/docs/APIDocs.html |
| `feat/nvidia-nim-support` | [#56](https://github.com/jdmanring/odysseus/issues/56) | Single commit, ready. File upstream issue first. |
| `fix/stream-429-backoff` | [#55](https://github.com/jdmanring/odysseus/issues/55) | Single commit, ready. File upstream issue first. |
| `fix/agent-context-budget-discovery` | [#54](https://github.com/jdmanring/odysseus/issues/54) | Test-coverage only (early-return preserved). File upstream issue first; note early-return removal is upstream-author decision. |
| `fix/agent-tool-budget` | [#10](https://github.com/jdmanring/odysseus/issues/10) | Single commit, ready |
| `fix/pytest-timeout-dependency` | [#6](https://github.com/jdmanring/odysseus/issues/6) | Single commit, ready |
| `fix/searxng-json-docs` | [#8](https://github.com/jdmanring/odysseus/issues/8) | Single commit, ready |
| `refactor/assets-move` | [#19](https://github.com/jdmanring/odysseus/issues/19) | Single commit, ready. Rework complete: SVG → `static/icons/`, manifest SVG entry added, build-macos-app.sh updated. |
| `fix/dom-oom-virtualization` | [#2](https://github.com/jdmanring/odysseus/issues/2) | 8 commits. Full three-phase virtualization + Phase 2 _evictLive fix + `hljsDeferForgetNode` calls in all 4 pruning paths (`_pruneTop` main + boundary, `_evictLive`, `_pruneBottom` main + boundary). 109 static-analysis tests in `test_chat_history_js.py` + 11 parametrized Playwright functions. **More-complete alternative to open upstream PR #4661, with an independent architecture** (MessageWindow design predates #4661: first commit `31d0bbb5` 2026-06-20 01:42 UTC, ~19h before #4661 opened 2026-06-20 21:07 UTC). **Precise provenance:** the per-node teardown cleanup was *adapted from #4661 and extended* (`memory-explosion-research.md` lines 365/527; draft line 58) — do not claim full code-independence. Ours additionally releases `_streamRenderer`/IntersectionObserver(`_sObs`)/hljs-defer refs and adds bidirectional scroll-up; #4661 is the smaller patch. Branch contamination (PR-draft .md) removed. **Plan to make it submittable: `docs/fork/plans/dom-oom-virtualization-upstream-plan.md`.** |
| `feat/aria2c-downloader` | [#12](https://github.com/jdmanring/odysseus/issues/12) + [#23](https://github.com/jdmanring/odysseus/issues/23) | Single commit, ready. Verified 2026-06-12: pause/resume, split-file, zombie detection, clear-finished, resume spinner, cancel. Windows buffering fix in — untested. |
| `feat/catppuccin-theme` | [#30](https://github.com/jdmanring/odysseus/issues/30) | Single commit, ready |
| `feat/ai-documentation-system` | [#18](https://github.com/jdmanring/odysseus/issues/18) | Single commit, ready |
| `fix/gpu-compositor-flicker` | [#32](https://github.com/jdmanring/odysseus/issues/32) | Single commit, ready |
| `fix/css-render-perf` | [#33](https://github.com/jdmanring/odysseus/issues/33) | Single commit, ready |
| `feat/gh-cli-detection` | [#5](https://github.com/jdmanring/odysseus/issues/5) | Single commit, ready. Detects gh CLI and injects GitHub context into agent system prompt; module-level cache ensures subprocess is called once per server lifetime; exports GH_TOKEN for keyring-auth systems. |
| `fix/tool-code-pycall-parsing` | [#35](https://github.com/jdmanring/odysseus/issues/35) | Single commit, ready. Parses and strips `<tool_code>` Python-call format (Google Gemma style) in tool_parsing.py. |
| `fix/gguf-quality-scored` | [#24](https://github.com/jdmanring/odysseus/issues/24) + [#29](https://github.com/jdmanring/odysseus/issues/29) | Single commit, ready. File after `feat/aria2c-downloader` (extends HfUrlResolver with GGUF discovery). |
| `fix/longcat-tool-parsing` | [#38](https://github.com/jdmanring/odysseus/issues/38) | Single commit, ready. |
| `feat/logging` | [#31](https://github.com/jdmanring/odysseus/issues/31) | Single commit, ready. Infrastructure + timing callsites combined — callsites are untestable without infrastructure. Replaces feat/logging-core and feat/logging-timing. |
| `fix/basicsr-python314-compat` | [#9](https://github.com/jdmanring/odysseus/issues/9) | Single commit, ready. Previously marked superseded; re-activated 2026-06-15. #3741 covers only the Serve panel; ours also covers the Dependencies tab and adds an inline POSIX abort. File upstream issue first. |
| `fix/untrusted-tool-result-header` | [#48](https://github.com/jdmanring/odysseus/issues/48) | 2 commits — initial scope fix (`a6fcc3e3`), improved header language (`484e9702`). Removes "reference material only" phrase, adds explicit "use it to complete the request" instruction, narrows injection warning to embedded *instructions*. Applied to develop as `db011acc`. File upstream issue first. |
| `fix/api-token-utcnow-deprecated` | [#51](https://github.com/jdmanring/odysseus/issues/51) | Single commit, ready. Follow-up to upstream 790ef81b — missed instance of deprecated datetime.utcnow() in api-token last_used_at update. File upstream issue first. |
| `fix/chat-auto-scroll-threshold` | [#49](https://github.com/jdmanring/odysseus/issues/49) | Single commit, ready. Adaptive viewport-based threshold replaces rigid 300px guard in _smoothScrollStep(). File upstream issue first. Rebuilt clean from contaminated `fix/chat-auto-scroll-bottom`. **Base of `fix/chat-stick-to-bottom`.** |
| `feat/thinking-overlay` | [#133](https://github.com/jdmanring/odysseus/issues/133) | Single commit (`9d959850`) from `upstream-mirror`; cherry-picked to develop (`163cbc52`, `-x`). Thinking indicator as a zero-footprint sticky overlay (height:0 sticky anchor + absolute bubble): bottom edge never moves on show/replace/remove (measured), indicator visible when scrolled up, role=status, no compositor-layer properties, `agent-thinking-dots` contract preserved. 7 static guards. Plan 3.3. PR draft at `docs/fork/upstream/pr-drafts/feat-thinking-overlay.md`. File upstream issue first. |
| `fix/chat-stick-to-bottom` | [#104](https://github.com/jdmanring/odysseus/issues/104) | **Stacks on `fix/chat-auto-scroll-threshold` (#49)** — its pin threshold matches the #49 follow distance. One observer is the source of truth for staying pinned: re-pins on any geometry change while pinned (`isPinned` flag, read pre-growth), covering late layout growth (image decode, highlight reflow, final block) and the mid-stream Thinking-box shrink/grow. Defers only while the lerp is actively animating (`_scrollRafId`), not the full throttle window. `MutationObserver` + per-child `ResizeObserver` because `#chat-history` messages are direct children with no wrapper. 8 tests in `tests/test_chat_stick_to_bottom_js.py`. **Supersedes `fix/chat-thinking-snap` (the `scrollHistorySettle` approach), now retired** — folded into the observer. Commit `94d9207d`, cherry-picked to develop (`a1033a97`, `-x`); #104's settle reverted on develop (`a660aedf`). Key finding: `autoScrollEnabled` was a dead gate (set true at submit, never cleared). Rationale, prior art + citations, lessons: `docs/fork/chat-scroll-research.md`. **Behavioural verification: Thinking-box path confirmed** (long output, multiple mid-stream Thinking popups held the bottom, 2026-06-25). ResizeObserver child-resize path (highlight reflow / image decode) still to confirm — repro: long fenced code block. Image *generation* is inpaint-scoped and cannot be used. File upstream issue first; stack on #49's PR. |
| `perf/hljs-deferred-highlight` | [#66](https://github.com/jdmanring/odysseus/issues/66) | 2 commits: deferred highlight (`a228e3ef`), `forgetNode()` export + window registration + 13 tests (`5fafd8cc`). Introduces `hljsDefer.js` (IntersectionObserver-based deferred highlighting) with `forgetNode(container)` to release observer refs before DOM eviction. 13 static-analysis tests in `tests/test_hljs_defer_js.py`. File upstream issue first (issue filed). |
| `fix/qtwebengine-oilpan-gc` | [#67](https://github.com/jdmanring/odysseus/issues/67) [#69](https://github.com/jdmanring/odysseus/issues/69) | 3 commits: initial deferred GC (`20dc1cde`), stacked-cycle guard + `[GC]` log line (`841670d1`), embedded-Chromium comment + 9 tests (`c75f4e4d`). **Superseded by `perf/agent-gc-catchup` on develop** — that branch includes all tests from this one plus the catch-up mechanism. File upstream issue first (issues filed). |
| `perf/agent-gc-catchup` | [#80](https://github.com/jdmanring/odysseus/issues/80) | 1 commit (`163f946c`). Adds `_gcMissed` catch-up flag to the GC scheduling block: when a response completes while GC is running, `_gcMissed = true`; after primary cycle completes, fires one catch-up GC cycle. Reduces lockout 5000ms → 3000ms. Logs `[GC] blocked — catch-up queued` and `[GC] catch-up dispatched`. 14 static-analysis tests in `tests/test_chat_gc_hint_js.py` (replaces 9 from `fix/qtwebengine-oilpan-gc`). PR draft at `docs/fork/upstream/pr-drafts/perf-agent-gc-catchup.md`. File upstream issue first. |
| `perf/gc-rendertail-instrumentation` | [#68](https://github.com/jdmanring/odysseus/issues/68) | 2 commits: counter (`7ea6ad0f`), allocation-pressure comment + 6 tests in `test_streaming_renderer_js.py` (`ab110206`). Framing: renderTail() fires once per SSE token; _rtCalls measures holder-div allocation pressure. File upstream issue first (issue filed). |
| `perf/streaming-final-render` | [#71](https://github.com/jdmanring/odysseus/issues/71) | 2 commits: fast-path (`fcc4e6df`), 7 tests in `test_chat_streaming_finalize_js.py` (`6086899c`). File upstream issue first (issue filed). |
| `perf/tool-bubble-inplace` | [#72](https://github.com/jdmanring/odysseus/issues/72) | 2 commits: in-place patch (`45b0344e`), 11 tests in `test_chat_tool_bubble_js.py` (`bc32709d`). File upstream issue first (issue filed). |
| `fix/tool-bubble-timer-leak` | [#73](https://github.com/jdmanring/odysseus/issues/73) | Single commit (`ef8b82e4`). Moves `_waveInterval`/`_elapsedTicker` cleanup before `_isBg` guard; adds `isConnected` check to skip silently-failed writes after `_evictLive`. Cherry-picked to develop. File upstream issue first. |
| `perf/agent-finalize-in-place` | [#74](https://github.com/jdmanring/odysseus/issues/74) | 2 commits: in-place finalize (`2c38aaf3`), logging + test (`ccd93c13`). When `_liveReplyEl._streamRenderer` exists, calls `update(_finalReply)` + `finalize()` instead of discarding the streamed DOM tree via `innerHTML = mdToHtml()`. 8 static-analysis tests in `tests/test_chat_live_reply_finalize_js.py`. PR draft at `docs/fork/upstream/pr-drafts/perf-agent-finalize-in-place.md`. File upstream issue first. |
| `perf/rendertail-text-only-path` | [#75](https://github.com/jdmanring/odysseus/issues/75) | 2 commits: text-only path (`751ef499`), test gap closures (`9733d2e1`). Adds `_lastTailText` tracking to `streamingRenderer.js`; skips holder-div creation for plain prose token appends by calling `.appendData(suffix)` on the live text node directly. 14 static-analysis tests in `tests/test_streaming_renderer_text_path_js.py`. PR draft at `docs/fork/upstream/pr-drafts/perf-rendertail-text-only-path.md`. File upstream issue first. |
| `fix/continue-btn-weakref` | [#78](https://github.com/jdmanring/odysseus/issues/78) | Single commit (`d3ba512c`). WeakRef closures on all 3 continue-button click handlers. Site 2 (step-limit button in `_chatBox`) is a confirmed GC root — without WeakRef the closure retains the evicted holder indefinitely. Sites 1 and 3 are defensive. 9 static-analysis tests in `tests/test_chat_continue_btn_js.py`. PR draft at `docs/fork/upstream/pr-drafts/fix-continue-btn-weakref.md`. File upstream issue first. |
| `perf/rewrite-streaming-renderer` | [#79](https://github.com/jdmanring/odysseus/issues/79) | 2 commits: renderer path (`04eea77f`), logging (`1d8bdd83`). `rewriteWith()` was doing `bodyEl.innerHTML = processWithThinking(newText)` on every SSE delta — O(n²) total. Lazy-init `createStreamRenderer` on first token, `.update()` per token, `.finalize()` + single final `innerHTML` after strip. Logs `[chat] rewrite: renderer finalized`. 11 static-analysis tests in `tests/test_chat_rewrite_streaming_js.py`. PR draft at `docs/fork/upstream/pr-drafts/perf-rewrite-streaming-renderer.md`. File upstream issue first. |
| `perf/round-finalize-inplace` | [#77](https://github.com/jdmanring/odysseus/issues/77) | 2 commits: in-place fixes (`1ee51846`), logging (`06cb0a2e`). Two fixes: (1) at `tool_start`, if `_contentEl3._streamRenderer` is live, call `.finalize()`+null in-place instead of `innerHTML`; (2) at final completion, if `.stream-content` already has child nodes, inject sources/findings as siblings instead of wiping `_body4.innerHTML`. Eliminates ~10 DOM rebuilds per 5-round session. Logs `[chat] round-finalize: tool_start in-place` and `sources in-place`. 13 static-analysis tests in `tests/test_chat_round_finalize_js.py`. PR draft at `docs/fork/upstream/pr-drafts/perf-round-finalize-inplace.md`. File upstream issue first. |
| `perf/smooth-typing` | [#81](https://github.com/jdmanring/odysseus/issues/81) | 1 commit (`4ba75a26`). Replaces clone-based `autoResize` in `ui.js` with `requestAnimationFrame`-coalesced `height:'auto'` + `scrollHeight` measurement. Eliminates 2 forced layout reflows per keystroke; N keystrokes per 16 ms frame → 1 reflow. 5 static-analysis tests in `tests/test_ui_auto_resize_js.py`. PR draft at `docs/fork/upstream/pr-drafts/perf-smooth-typing.md`. File upstream issue first. |
| `perf/gc-micro-improvements` | [#82](https://github.com/jdmanring/odysseus/issues/82) | 1 commit (`d2100d87`). `squashOutsideCode` fast path: `str.includes('```')` short-circuit avoids split/join allocation for plain-text responses (~30 fps during streaming). 3 static-analysis tests in `tests/test_markdown_squash_js.py`. PR draft at `docs/fork/upstream/pr-drafts/perf-gc-micro-improvements.md`. File upstream issue first. |
| `perf/chathistory-gc-improvements` | [#83](https://github.com/jdmanring/odysseus/issues/83) | 2 commits from `upstream-mirror`: base chatHistory.js (`337fedc5`), GC improvements (`d05e9e2e`). rIC after `_evictLive` and `_pruneBottom`; full `_waveInterval`/`_elapsedTicker`/`_streamRenderer` teardown in all four removal loops of `_pruneTop` and `_pruneBottom`; `_purgeStaleBackgroundStreams()` at top of `checkBackgroundStream`. 5 static-analysis tests (+4 in `test_chat_history_js.py`, +1 in `test_chat_gc_hint_js.py`). PR draft at `docs/fork/upstream/pr-drafts/perf-chathistory-gc-improvements.md`. File upstream issue first. |
| `fix/skill-extraction-threshold` | [#84](https://github.com/jdmanring/odysseus/issues/84) | 1 commit (`5b8c0364`). `MIN_CONFIDENCE` 0.6 → 0.85 (aligns with injection floor); gate changed from OR to AND with tools ≥ 3 (was tools ≥ 2); `auto_approve_skills` default `True` → `False`. `routes/chat_helpers.py` outer gate updated to match. 5 tests in `tests/test_skill_extraction_gate.py`. Issue drafts and PR drafts at `docs/fork/upstream/`. File upstream issue first. |
| `fix/skill-agent-prompt-language` | [#85](https://github.com/jdmanring/odysseus/issues/85) | 1 commit (`383fbc6f`). Removes "BEFORE doing domain work", "proven to work", "Follow them step by step", "treat them as authoritative guidance", "consult before doing domain work" from `src/agent_loop.py`. Replaces with conditional/advisory language. 6 tests in `tests/test_agent_skill_prompt_language.py`. References upstream #2750. File upstream issue first. |
| `fix/skill-lifecycle-correctness` | [#86](https://github.com/jdmanring/odysseus/issues/86) | 1 commit (`126e1b62`). Corrects `auto_approve_skills` semantics across skill pipeline: extraction always draft (pref check removed); injection path restored to `True` default with source-aware pre-filter replacing `min_conf=2.0` hack; audit path `True` (audit promotes on pass); `manage_skills add` always draft. Teacher confidence 0.8→0.9 (2 locations). 11 tests in `tests/test_skill_lifecycle_correctness.py`. Research: SkillsBench arxiv:2602.12670, SkillWeaver arxiv:2504.07079. Architecture doc: `docs/dev/skill-system-architecture.md`. Plan: `docs/fork/plan-skill-lifecycle.md`. File upstream issue first. |
| `feat/skill-quality-signals` | [#87](https://github.com/jdmanring/odysseus/issues/87) | 1 commit (`921327ec`). BM25 hybrid retrieval: replaces pure Jaccard in `get_relevant_skills()` with `0.5*Jaccard + 0.5*BM25_norm`; IDF cached on `SkillsManager._idf_cache`, invalidated on library mutations. Composite health score (0–100) derived from `confidence` (40 pts), `audit_verdict` (30 pts), `uses` (20 pts), `necessity` (10 pts); `health_score` field in `load_all()` API response; color-coded badge on Brain > Skills cards. 7 tests in `tests/test_skill_retrieval_bm25.py`. Research: SkillRet arxiv:2605.05726, Skill Retrieval Benchmark arxiv:2604.24594, SkillOps arxiv:2605.13716. File after `fix/skill-lifecycle-correctness`. |
| `perf/hljs-deferred-highlight` | (extends existing branch) | 1 new commit (`655be16f`). Replaces all 7 remaining direct `window.hljs.highlightElement` calls in `chat.js` with `deferHighlightAll(container)`. 3 static-analysis tests in `tests/test_chat_hljs_defer_js.py`. File upstream issue first. |
| `feat/qt-native-linux-app` | [#14](https://github.com/jdmanring/odysseus/issues/14) | 14 commits. `javaScriptConsoleMessage` routes JS console output to wrapper_system.log and audits CDP listener counts after Phase 2 eviction. `renderProcessPid()` replaces `pgrep` subprocess in the 60 s memory poll. `ThreadPoolExecutor(max_workers=2)` bounds CDP background threads; unblocks PSI poll loop. `_rotate_log()` rotates `wrapper_system.log` and `server_access.log` at startup before `os.dup2` with 5-backup shift loop (10 MB; matches app `RotatingFileHandler`). Fix: `nodes` NameError in threshold-gc path (cherry-pick conflict). Log quality: reason params, success/failure, VmPeak on new peak, BRIDGE tag on color portal. **Perf (commit `7d288485`):** removed `_cdp_purge_memory` (stop-the-world); replaced with async JS `gc()` via `page.runJavaScript`; 500 ms debounce on focus-loss; 30 s PSI cooldown; 250 ms drain timer for cross-thread dispatch. **Memory flags (commit `e67acfe2`):** `--initial-old-space-size=128`, `--optimize-for-size`, `--minor-mc`, `--renderer-process-limit=1`, `--disable-extensions`. **Tile eviction (commit `16c5d10f`):** `Memory.simulatePressureNotification(moderate)` added to 60 s poll — Qt WebEngine doesn't forward OS pressure to cc::TileManager, so hover rasterization accumulates without eviction; this fires the same MemoryPressureListener path the OS would use. **P1–P5 perf (commit `e237696c`):** `--enable-low-end-device-mode` caps cc::TileManager raster budget ~96 MB; HTTP cache capped at 50 MB; page lifecycle freeze on minimize via `setLifecycleState(Frozen/Active)`; periodic eviction timer reduced 10 s → 30 s; RSS before/after eviction telemetry. 6 new tests in `tests/test_qt_cdp_listener_audit.py` (`ce27c0a6`). 54 total static-analysis tests. PR draft at `docs/fork/upstream/pr-drafts/feat-qt-native-linux-app.md` (updated). js-flags fix (commit `14928a5d`): comma-separated `--js-flags` so all V8 heap flags reach renderer. Pending: #91 (V8 telemetry + host RSS + HeapProfiler.collectGarbage). File upstream issue first. **⚠ 2026-06-26 FOLD — this row predates the memory fold and several items below are now superseded.** The branch now carries the full memory stack (#106/#112/#116/#120, ~28 commits, backup `backup/feat-qt-native-linux-app-prefold`). Superseded since this row was written: `simulatePressureNotification` was **removed** (measured no-op on QtWebEngine — #106) in favour of gated `forciblyPurgeJavaScriptMemory`; `--enable-low-end-device-mode` was **removed** (#96, raster tint, didn't bound the Oilpan pool); the flat `some>5%`/30 s-cooldown PSI trigger is replaced by the graduated `qt_psi.py` monitor; `_request_async_gc`/`_gc_request_pending` are gone. Current memory design is in the rewritten PR draft and `docs/fork/plans/psi-graduated-reclaim-plan.md`. |
| _(pending ingest — upstream PR #4661)_ | — | "fix(ui): prevent browser OOM during long agent interactions" by holden093. Open, not yet merged. Safe pieces pre-applied to develop (67268d2b): `_purgeStaleBackgroundStreams()`, `?limit=400` history pagination, `streamDocDelta()` rAF throttle, `content-visibility:auto` CSS (49ce03bb). **Do not take `_trimChatHistoryDOM()` or `_loadOlderMessages()` during ingest** — incompatible with chatHistory.js virtualization; our `_evictLive()` replaces them. Attribution: adapted from PR #4661 (holden093). Test branches: `test/pr-4661`, `test/upstream-pr-4661`. |

---

## Superseded / Closed

| Issue | Branch | Notes |
|-------|--------|-------|
| [#94 opt-in low-end-device-mode toggle](https://github.com/jdmanring/odysseus/issues/94) | — | Closed 2026-06-25 — premise falsified. The toggle would expose `--enable-low-end-device-mode`, but that flag (a) causes the lighter-rectangle raster tint and (b) does not bound the hover-OOM (Oilpan `:hover` pseudo-element churn, a separate pool). Real fixes: #96 (drop the flag), #97 (idle GC). Remote access split to #95. |
| [#37 LongCat tool_call parsing](https://github.com/jdmanring/odysseus/issues/37) | — | Closed — investigation subsumed by #38 (`fix/longcat-tool-parsing`). Implementation handles all format variants. |
| [#7 HF token not saved outside Cookbook tab](https://github.com/jdmanring/odysseus/issues/7) | `fix/hf-token-persistence` | Closed 2026-06-12 — superseded by upstream #3459 (`load_stored_hf_token()` in cookbook_helpers.py) |
| [#34 HF token env fallback](https://github.com/jdmanring/odysseus/issues/34) | `fix/hf-token-env-fallback` | Closed 2026-06-12 — same upstream fix covers this |
| [#11 streamingTTS scope](https://github.com/jdmanring/odysseus/issues/11) | `fix/streamingtts-scope` | Superseded — upstream PR #2418 ("fix(chat): render abort messages — hoist streamingTTS out of the try block") makes same hoist fix with broader scope and a test. Still open as of 2026-06-15, no comments. Delete branch once #2418 merges into upstream-mirror. |
| [#47 workspace shell access](https://github.com/jdmanring/odysseus/issues/47) | `fix/workspace-shell-access` | Closed 2026-06-18 — superseded by upstream PR #4366, which pins bash/python/web_search/web_fetch/read_file/write_file/edit_file to ALWAYS_AVAILABLE system-wide. Temporary fork fix on develop (commit d8c7fa21) until #4366 merges. |
| [#4 tool result role confusion](https://github.com/jdmanring/odysseus/issues/4) | `fix/tool-result-role` | Closed 2026-06-18 — superseded by upstream #1629 (commit `4e477741`), which wraps non-native tool results via `untrusted_context_message()`. Dead code removed from develop in `eda573e1`. |
| [#39 Google compat camelCase toolCalls](https://github.com/jdmanring/odysseus/issues/39) | `fix/google-compat-toolcalls` | Closed 2026-06-18 — premise disproved by live API test. Google's compat endpoint sends snake_case `tool_calls` per spec. Branch and test file deleted. A related real deviation (`finish_reason: "stop"` instead of `"tool_calls"`) does not affect Odysseus; tracked in #52. |
| [#70 renderTail rAF throttle](https://github.com/jdmanring/odysseus/issues/70) | `perf/rendertail-raf-throttle` | Closed 2026-06-21 — superseded by `fix/dom-oom-streaming-throttle` (#64), which implements an identical rAF guard (`_renderRafId`) at the same call site with tests. |

---

## Fork-Only Work

| Issue | Branch | Notes |
|-------|--------|-------|
| [#15 Upstream sync pipeline](https://github.com/jdmanring/odysseus/issues/15) | `feat/upstream-sync-pipeline` | Manages fork/upstream relationship — not applicable upstream |
| [#131 staged-branch convergence guard](https://github.com/jdmanring/odysseus/issues/131) | `test/staged-branch-convergence-guard` | **Done, closed.** `tests/test_staged_branch_convergence.py` fails develop's suite when the staged #2 branch's four converged files drift from the maintained versions (whole-line comments excluded — the fork-reference scrub is comment-only by contract). Normalizer mutation-checked; red-verified against the real pre-convergence state (`661be326`); skips where the branch is absent. The generic mechanism ships upstream via the bench snapshot-drift guard (#128); this application names fork staging branches → fork-only, **export candidate for fork-workbench-template**. Companion rule: exit-code merge gate in `docs/fork/testing.md`. |

## Branch-hygiene notes

- **`fix/brain-panel-oom` (#108) ↔ `fix/gpu-compositor-flicker` — coupling RESOLVED.**
  `test_gpu_compositor_flicker_css.py` had duplicated three memory-synapse-sweep
  tests (a `fix/brain-panel-oom` concern, already covered by
  `test_brain_panel_oom_css.py`), which coupled the two unrelated contributions
  (changing the sweep in #108 broke a test in the gpu-flicker file). Fixed: the
  duplicated tests were removed (develop `a1c5d594`) so the file guards only the
  backdrop-filter removals, and the now single-concern test was placed on its own
  branch (`fix/gpu-compositor-flicker` `c5cb6366`) so that PR is self-contained.
  The branches are cleanly disjoint (backdrop-filter removal vs animation OOM) and
  must stay separate — one thing per PR.
- **`tooling/mem-probe.py`** is fork tooling (develop-only by design); it depends
  on the Qt wrapper (port 9222) and would ship with the wrapper feature if offered
  upstream.
