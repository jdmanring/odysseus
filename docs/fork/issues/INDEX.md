# Issue index

143 open, 36 closed. Generated by `tooling/issues_to_markdown.py`.

`#N` in the fork docs refers to a row here. Full bodies and comment threads are in `issue-export.json`; run the renderer for the readable version (`README.md`, generated, not committed).

| # | State | Title |
|---|-------|-------|
| 180 | open | integration has been bypassed since 2026-07-19; the pipeline can no longer advance it (fork-only) |
| 179 | closed | Post-ingest checklist: the steps that only exist in one session's memory (fork-only) |
| 178 | open | Two dead artifacts removed: Aria2Wrapper and timed_operation |
| 177 | open | 33 staged branches have no PR draft (fork-only tracking) |
| 176 | open | Source-assertion tests miss structural breakage in style.css and the tool allowlist (upstream-candidate) |
| 175 | open | Session-route tests pick the wrong endpoint from a module-global router (upstream-candidate) |
| 174 | open | Test suite never removes its temp databases or directories (upstream-candidate) |
| 173 | open | Context-window lookup matches the org prefix instead of the model name (upstream-candidate) |
| 172 | open | Split hybrid recall + write-time supersede out of the Qdrant branch (upstream-candidate) |
| 171 | open | Upstream ingest 2026-08-02 — 182 conflicted files, merged as b2e97adb |
| 170 | closed | Land the ingest merge tooling on develop (an aborted merge currently destroys it) |
| 169 | closed | Edit/regenerate/fork derive keep_count from DOM index, not DB position → wrong truncation/fork point (pre-existing; make id-based) |
| 168 | closed | Streaming text-only fast path: renders ~~strikethrough~~ literally + ships a per-message debug console.log |
| 167 | closed | Background-stream error notice never shown — checkBackgroundStream purges the entry before its own error branch runs |
| 166 | closed | Qdrant adapter missing _Collection.update() → username rename silently orphans a user's personal RAG docs |
| 165 | closed | Landing welcome screen invisible — #chat-history containment layer occludes the un-z-indexed #welcome-screen |
| 164 | closed | New Chat / /clear / archived-view skip MessageWindow.reset() → stale header count + window mis-tracking (#2 gap) |
| 163 | open | fix(db): import declarative_base/declared_attr from sqlalchemy.orm (ext.declarative is deprecated) |
| 162 | closed | fix(document): streamDocFinalize skips final content when only one editor element is present |
| 161 | open | Memory overhaul: Qdrant vector store + fully-optimized nomic (256-dim MRL, prefixes, 8K-sized chunks) |
| 160 | open | Installers: verify the memory/RAG stack at install time + auto-install native prerequisites |
| 159 | open | Desktop tray: server-runner menu + native About dialog (Linux/Windows/macOS) |
| 158 | open | macOS wrapper polish: close-to-Dock lifecycle, native Edit menu, multi-res icns, ad-hoc codesign |
| 157 | open | Eyedropper on GPU-less Macs kills WindowServer (ColorSampler → SkyLight software-capture abort) |
| 156 | open | fix(ui): modal z-promote MutationObserver loops to renderer OOM when OS reduce-motion is on |
| 155 | open | fix(theme): canvas background effects ignore prefers-reduced-motion |
| 154 | open | Static asset ?v= cache-busters should derive from content hashes at serve time |
| 153 | open | vLLM serve fails on desktop/toolkit-less machines: free-VRAM refusal undiagnosed, flashinfer JIT requires nvcc |
| 152 | open | Per-frame decoration cost: full-screen backdrop-filter flicker in QtWebEngine, standing will-change layers |
| 151 | open | HW-Fit list: sort click refetches everything (lag); quant filter leaks Q4_K_M Ollama rows |
| 150 | open | HW-Fit lists models no engine can execute — fit means VRAM, never architecture servability |
| 149 | open | HW-Fit fabricates a Q4_K_M/llama.cpp identity for plain safetensors models with no GGUF |
| 148 | open | GGUF resolver substitutes a completely unrelated model when no true quant exists |
| 147 | open | Windows aria2c download path: PowerShell port, verified on the win11 VM |
| 146 | open | aria2c downloader regression: launcher deleted, all downloads silently on hf_transfer since June 15 |
| 145 | open | fix(chat-scroll): stick-to-bottom unpin threshold is ~1.5 viewports — wheel scroll-up cannot escape |
| 144 | open | feat(settings): chat column width preference (default 800px ≈ 63 mono chars) |
| 143 | open | fix(shortcuts): Ctrl+, resurrects the last tool window (Brain) instead of opening Settings |
| 142 | open | test(chat-history): longsession Playwright auto-follow flake after 2026-07-19 ingest rebase |
| 141 | open | fix(chat-history): Phase 2 prune check races stick-to-bottom autoscroll — prune can defer indefinitely |
| 140 | open | feat(images): free remote image generation — Pollinations (keyless) + Cloudflare Workers AI adapter for generate_image |
| 139 | closed | fix(cookbook): restore Coding/Reasoning/Chat scan categories — backend still fully supports them |
| 138 | open | Header msg counter counts DOM blocks: undercounts under virtualization (regression in staged branch), mislabels rounds as msgs |
| 137 | open | Renderer main-thread hang (futex wait in QtWebEngineCore) after opening old session with stale stream state |
| 136 | open | Undeclared test/optional deps: python-magic and python-docx leave 3 tests skipping |
| 135 | open | NameError in hf_gguf_files error path: undefined 'repo' (upstream fbdec22d) |
| 134 | closed | chat_stream 500s: NameError _explicit_web_intent (upstream 264da651 removed the definition, kept 3 reads) |
| 133 | open | feat(chat): render the Thinking indicator as a zero-footprint sticky overlay (no bottom-edge movement) |
| 132 | open | fix(chat-history): scroll-down batch renders below the bottom honesty spacer — message order corrupted |
| 131 | closed | test(fork): staged-branch convergence guard — staged chatHistory.js must not drift from the maintained version |
| 130 | open | fix(chat-history): scroll-up paging dead-ends at the top — one-shot IntersectionObserver fire swallowed during _loading is never re-triggered while the viewport stays pinned |
| 129 | open | fix(chat-history): DOM grows unbounded during sustained scroll-up in the real app — MessageWindow never prunes on the paging path (1,980 nodes at n=2000) |
| 128 | open | bench(chat-history): reproducible virtualization benchmark comparing naive / #4998 detach / MessageWindow eviction / hybrid |
| 127 | open | fix(chat-history): scrollbar misrepresents history — MessageWindow renders no spacer for unrendered older messages (thumb shows 2.5% of a 2000-message chat) |
| 126 | open | bench(chat-history): hybrid arm's top-spacer height drifts — deep scroll-back excursion stalls at 114/200 messages |
| 125 | open | History pagination never runs: legacy /api/history route shadows the paginated handler |
| 124 | closed | Diagnose: stale localhost JS sometimes needs a full restart despite _RevalidatingStatic no-cache |
| 123 | open | Persist the catalog↔download association for discovered models (retire the client base-name heuristic) |
| 122 | open | Add API Models provider list not alphabetical (static option order drifts as providers are appended) |
| 121 | open | Recurring regression: installed models don't grey out — duplicated downloaded-detection matcher (~16 sites, 3 files) |
| 120 | closed | qt_wrapper PSI monitor: graduated some/full response with validated, tunable thresholds |
| 119 | open | perf(document): _sigCache signature data-URL cache is unbounded |
| 118 | open | perf(quiescence): visibility-gate perpetual background timers (modalManager scan, email/tasks polls) |
| 117 | open | perf(quiescence): ambient decorative animations never pause — adopt an idle-quiescence rule |
| 116 | open | resilience(qt): forcing --enable-gpu-rasterization turns a GPU-driver failure into a 12-core CPU meltdown that freezes the UI |
| 115 | open | perf(research): orbit ring repaints the whole pane every frame, forever (perpetual conic-gradient + mask raster) |
| 114 | open | perf(host): in-process-GPU high-water mark — host RSS grows ~40% under use, not reclaimed by renderer purge |
| 113 | open | perf(server): drop uvicorn --access-log for the embedded single-user deployment |
| 112 | open | diagnostics(mem): host browser process (in-proc GPU+net) growth unmeasured — add host VmRSS telemetry |
| 111 | open | perf(mcp): lazy-connect cold built-in MCP servers (image_gen/rag/email) on first use |
| 110 | open | Tasks-panel clock re-rasters the whole modal layer every second (~1.7 MB/s); isolate it to its own layer |
| 109 | open | UI unresponsive after minimize/restore: page freeze (setLifecycleState Frozen) loses input on thaw |
| 108 | open | Brain/Notes panel CSS animations generate continuous raster tiles; compositor-promote and make memory sweep hover-triggered |
| 107 | open | Whirlpool spinner rAF loop never terminates when element is never appended (or is hidden): perpetual CPU + canvas churn |
| 106 | open | Renderer memory climbs unbounded: reclaim calls simulatePressureNotification, a no-op on QtWebEngine (use forciblyPurgeJavaScriptMemory) |
| 105 | open | Chat image generation is undiscoverable: settings are inpaint-scoped and don't expose a working provider/model |
| 104 | open | fix(chat): snap-to-bottom drops out across the Thinking-box grow/shrink/grow transition |
| 103 | open | fix(chatHistory): scroll-down drains to bottom instead of acting as the inverse of scroll-up |
| 102 | open | feat(editor): persist undo history across sessions (bounded; deferred) |
| 101 | open | fix(editor): editor save can create a 0-byte broken gallery image (no empty-output guard) |
| 100 | open | fix(editor): Ctrl+Shift+Z (redo) shortcut dead — e.key is 'Z' uppercase when Shift held |
| 99 | open | perf(editor): PNG-compress older undo snapshots to cut edit-session memory peak |
| 98 | open | perf(ui): lazy-decode off-screen stacked/grid images (document pages, gallery draft thumbs) |
| 97 | open | perf(qt): idle-triggered GC to reclaim transient hover DOM churn (CSS :hover pseudo-elements) |
| 96 | open | fix(qt): remove --enable-low-end-device-mode — causes raster tint, doesn't bound the actual OOM |
| 95 | open | feat(server): network-reachable mode for remote access from tablets and other devices |
| 94 | closed | feat(qt): opt-in low-end-device-mode toggle (startup pref) for the desktop wrapper |
| 93 | open | fix(css): contain:paint on sidebar and chat-history breaks backdrop-filter and transparent compositing |
| 92 | open | fix(css): global hover raster tile accumulation — containment, animation, and transition fixes |
| 91 | open | perf(qt): add V8 heap telemetry, HeapProfiler.collectGarbage, and host-process RSS logging |
| 90 | open | fix(qt): --js-flags space-splitting drops all V8 flags except --expose-gc |
| 89 | open | fix(memory): Brain panel memory list accumulates event listeners and holds raster tiles |
| 88 | open | fix(css): scroll-hover transition accumulates raster tiles in Brain memory list |
| 87 | open | feat(skills): BM25 hybrid retrieval scoring + composite skill health score |
| 86 | open | fix(skills): auto_approve_skills semantics broken — extraction always draft, audit defaults to trust |
| 85 | open | fix(agent): skill prompt instructions create mandatory consultation loop and unconditional authority |
| 84 | open | fix(skills): extraction threshold too low and auto-approve default publishes unreviewed skills |
| 83 | closed | perf(chatHistory): idle yield after prune/evict, full teardown before remove, purge stale background streams on session switch |
| 82 | open | perf: reduce GC pressure — prune teardown, idle yield, squashOutsideCode fast path, deferred hljs highlight, background stream cleanup |
| 81 | open | perf: eliminate typing lag — async JS GC + rAF-coalesced autoResize |
| 80 | open | perf: improve GC scheduling for agent sessions — missed-GC catch-up |
| 79 | open | perf(chat): rewriteWith() does full markdown parse + DOM rebuild on every SSE token |
| 78 | open | fix(chat): continue-button closures retain evicted message holders via strong reference |
| 77 | open | perf(chat): multi-round agent tool_start discards streaming DOM tree via innerHTML reset |
| 76 | open | perf(qt): correlate jsEventListeners CDP counter with Phase 2 eviction events |
| 75 | open | perf(streaming): skip renderTail() parse for plain-text token appends |
| 74 | open | perf(chat): finalize live-reply renderer in-place instead of innerHTML replace on agent path |
| 73 | open | fix: tool_output timers leak when session switches to background |
| 72 | open | perf(streaming): patch tool bubble status in-place instead of innerHTML replace |
| 71 | open | perf(streaming): skip final innerHTML re-render for plain responses |
| 70 | open | perf(streaming): throttle renderTail DOM updates to rAF cadence |
| 69 | open | fix(perf): prevent stacked concurrent async GC cycles |
| 68 | open | feat(perf): add renderTail call counter and GC dispatch logging |
| 67 | open | fix(ui): QtWebEngine Oilpan collection failure — RSS grows unbounded without explicit GC trigger |
| 66 | open | perf(ui): defer hljs syntax highlighting for off-screen code blocks |
| 65 | closed | fix(dom): Phase 2 pruning hard-stops when history exhausted — live messages accumulate without bound |
| 64 | open | perf(streaming): fix O(n²) thinking-block allocation, rAF throttle, StreamRenderer teardown — OOM prevention |
| 63 | open | fix(llm): stream_options extension sent to providers that may not support it — exclusion list grown reactively without audit |
| 62 | open | fix(agent): _API_HOSTS incomplete — several provider endpoints not listed, degrading tool calling for non-keyword model names |
| 61 | open | fix(llm,agent): LongCat provider gaps — output truncated at 32 K, stream_options rejected by API |
| 60 | open | fix(agent): NVIDIA NIM models don't receive native tool schemas — integrate.api.nvidia.com missing from _API_HOSTS |
| 59 | open | fix(providers): Google Gemini logo ordering bug + Pollinations AI missing from provider dropdown |
| 58 | open | feat: Add LongCat as a full provider integration |
| 57 | open | Research: lazy context-length probe for api/proxy endpoints |
| 56 | open | feat/fix(nvidia): NVIDIA NIM endpoint missing curated model list; 69/91 models get wrong 6K context budget; one model provably wrong |
| 55 | open | fix(llm): HTTP 429 treated as hard failure in streaming path — no retry, no backoff, no Retry-After |
| 54 | closed | fix(agent): context budget locks at 6K for any model not in KNOWN_CONTEXT_WINDOWS, silently destroying session history |
| 53 | closed | fork/onboarding-docs: fork-specific onboarding companion to feat/ai-documentation-system |
| 52 | open | Google OpenAI-compat endpoint: finish_reason is "stop" instead of "tool_calls" when tools fire |
| 51 | open | fix(auth): api-token last_used_at uses deprecated datetime.utcnow() instead of utcnow_naive() |
| 50 | closed | Chat auto-scroll fails to follow bottom when large content updates occur |
| 49 | open | Chat auto-scroll fails to follow bottom when large content arrives |
| 48 | open | fix(agent): untrusted tool-result header causes model to refuse user requests citing "untrusted source" policy |
| 47 | open | fix(agent): bash/python/web_search absent from tool schemas on low-signal turns when workspace is set and tools are enabled |
| 46 | open | feat: native OpenBSD wrapper (PyQt6 / Qt WebEngine from ports) |
| 45 | open | feat: extend Qt wrapper to support FreeBSD (KDE Plasma) and GhostBSD |
| 44 | open | feat(windows): native Windows desktop application (PyQt6 wrapper) |
| 43 | open | feat: native macOS wrapper (pywebview / WKWebView) |
| 42 | open | feat(resolver): expand quality-scored source discovery to all backends and imatrix quant tiers |
| 41 | open | feat(tool_parsing): parse Gemma 4 raw tool-call format from local serving layers |
| 40 | open | refactor(llm_core): harden Google OpenAI-compat layer — payload builder, quirk isolation, test coverage |
| 39 | closed | llm_core: Google OpenAI-compat endpoint sends toolCalls (camelCase) not tool_calls |
| 38 | open | tool_parsing + agent: LongCat (Meituan) tool call support |
| 37 | closed | tool_parsing: <longcat_tool_call> format not parsed or stripped |
| 36 | open | remove hf_transfer: replace with aria2c downloader |
| 35 | open | tool_parsing: <tool_code> blocks with Python function-call syntax are not parsed or stripped |
| 34 | closed | HF token: fall back to HF_TOKEN env var when no stored token |
| 33 | open | CSS render performance pass: will-change cleanup, CSS containment, reduced-motion global, touch-action, hover fix |
| 32 | open | Black screen flicker on sidebar hover, dropdown open, and modal open — backdrop-filter on opaque interactive elements |
| 31 | open | feat: comprehensive structured logging system with debug mode |
| 30 | open | feat: add Catppuccin Mocha theme |
| 29 | open | GGUF source resolution returns low-quality results |
| 28 | closed | Filesystem tools crash at runtime — dead workspace import from c1674fc/0aba00f merge ordering |
| 27 | closed | fix: scroll behavior bugs in virtualised chat history |
| 26 | closed | Filesystem access broken: read_file never surfaced + path confinement blocks home directory |
| 25 | closed | fix(cookbook): cached model indicator doesn't update after GGUF download |
| 24 | open | feat: dynamic GGUF source discovery for llamacpp backend |
| 23 | open | aria2c downloader: reliability fixes and download card UI bugs |
| 22 | closed | AI_ONBOARDING.md — architecture primer for AI agents (upstream contribution) |
| 21 | closed | AGENTS.md — AI agent entry point should be upstream contribution |
| 20 | closed | BinManager test suite and fork testing standards |
| 19 | open | Move media assets from docs/ to assets/ — docs/ should be documentation only |
| 18 | open | AI-first documentation system — zero-processing comprehension for any AI agent |
| 17 | closed | QWebEngineView localStorage wipes on every exit — in-memory profile |
| 16 | open | Download UI overhaul — per-file progress rows, accurate overall progress |
| 15 | open | Upstream sync pipeline — automated 3-gate ingest from upstream/dev |
| 14 | open | Native Linux desktop application (Qt wrapper) |
| 13 | closed | Qt native color picker dialog — replace broken eyedropper button |
| 12 | open | Replace hf_transfer with aria2c for parallel model downloads |
| 11 | closed | streamingTTS ReferenceError aborts catch block on every stream error |
| 10 | open | agent_max_tool_calls defaults to 0 — agent mode non-functional out of box |
| 9 | open | realesrgan / basicsr broken on Python 3.14 |
| 8 | open | SearXNG search fails — JSON output not documented as required |
| 7 | closed | HF token not saved when set outside Cookbook tab |
| 6 | open | pytest-timeout not declared as a dependency |
| 5 | open | gh CLI is unusable in non-interactive environment |
| 4 | closed | Tool results misattributed as user messages — model wastes turns analyzing injected content |
| 3 | closed | External links and buttons don't navigate in Qt wrapper |
| 2 | open | Renderer OOM crash — unbounded DOM growth in chat history |
